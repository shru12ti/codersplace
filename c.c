#include <stdio.h>

int main() {
    int x = 10;
    int y = 0;
    int result;

    result = x / y;  // Division by zero error

    printf("The result is: %d\n", result);

    return 0;
}
